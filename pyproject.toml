[project]
name = "python-journey"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipykernel>=6.30.1",
    "ipython>=8.37.0",
]

[dependency-groups]
dev = [
    "ruff>=0.13.1",
]
